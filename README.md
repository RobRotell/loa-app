# Loa
Simple article tracker project to test out WordPress REST APIs, authentication tokens, and React.

Two parts:
* WordPress — headless CMS hosted on Siteground (contains DB)
* frontend — React SPA hosted on Digital Ocean
